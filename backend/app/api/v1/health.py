"""
File: health.py
Công dụng: Router check health status của API
- GET /ping: Kiểm tra server có chạy hay không
- GET /health: Check tổng trạng thái (DB, Redis, Vector DB, LLM)

Dùng để monitor service availability
"""
